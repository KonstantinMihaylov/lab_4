#include <stdio.h>
#include <malloc.h>

int main()
{
  int *arr;  
  int size;
  scanf("%d", &size);
  arr = (int*) malloc (size * sizeof(int));
  for (int i = 0; i < size; i++)
  {
    scanf("%d", &arr[i]);
  }
  for (int i = 0; i < size; i++)
  {
    printf("%d ", arri]);
  }
  free(arr);
  
  return 0;
}
